# Frontend tests

Dành cho rendering, accessibility, citation interactions, streaming/error states và mapping theo HTTP contract. Dùng fixtures synthetic, không gọi model thật.

End-to-end xuyên ba thành phần thuộc `tests/` ở root. Chưa có runner hoặc browser test cài đặt.
