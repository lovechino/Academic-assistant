# Backend application

Use case hội thoại, course/material lifecycle, review/approval và orchestration jobs. Phụ thuộc domain và ports; AI core được gọi qua adapter, không import graph/chunker trực tiếp.

Xử lý request identity, idempotency, budget/deadline và audit events ở đúng boundary. Chưa có implementation.
