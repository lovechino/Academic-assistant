# Backend tests

Dành cho request/response contract, quyền theo vai trò/môn, approval/revocation, idempotency và error/failure paths. Mock AI qua boundary, không cần model thật để chấm quyền.

Chưa có tests server/runtime. Bộ kiểm tra scaffold tại root chỉ kiểm tra tổ chức source.
