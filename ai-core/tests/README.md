# AI core tests

Nơi dành cho tests hành vi AI core: structural invariants, candidate grouping/packing, grounding/citation, scope enforcement, router và budget/failure paths.

Dùng synthetic/approved fixtures nhỏ. Gold/silver corpus giữ ở `data/evaluation/`, không nhúng vào source tests. Chưa có test runtime; kiểm tra cấu trúc repo không phải điểm AI.
